# Compressed RAM swap (zswap backing an xswap device) makes swapping cheap, so
# prefer reclaiming anonymous pages over dropping page cache. This preserves
# the value the zram udev rule used to set (SYSCTL{vm.swappiness}=150).
vm.swappiness = 150
